library ieee;
use ieee.std_logic_1164.all;
use ieee.std_logic_unsigned.all;

entity coincidence is
    port(
        a, b : in std_logic_vector(0 to 2);
        c : out std_logic
    );
end coincidence;

architecture arch_coincidence of coincidence is 
begin
    --process(a,b) is
    --begin
        c <= '1' when a = b else
				 '0'; 
				 
		  --if (a = b) then 
            --c <= '1';
        --else
            --c <= '0';
		  --end if;

    --end process;
end arch_coincidence;
